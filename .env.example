# Demo mode is ON by default (no Engine required).
# Set to false to POST/poll a live Engine instead.
NEXT_PUBLIC_DEMO_MODE=true

# Used only when NEXT_PUBLIC_DEMO_MODE=false
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
